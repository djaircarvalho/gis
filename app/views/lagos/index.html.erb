
<div id="map" class="mapa">
  <%=
    map(:center => {
      :latlng => [-5.360528731202039, -42.67536163330079],
      :zoom => 16
    })
  %>
</div>

<script type="text/javascript">

   var drawnItems = new L.FeatureGroup();
   var lagos = JSON.parse('<%=escape_javascript @lagos.to_json %>'.replace(/&quot;/g,'"'));

        
     var wkt = new Wkt.Wkt();
     
    for (var i = 0; i < lagos.length; i++) {
        var lago = lagos[i];
        var obj = wkt.toObject(wkt.read(lago.geom));
        obj.setStyle({
          color: 'blue'
      });
        obj.addTo(map);
      }  
     
</script>
<p id="notice"><%= notice %></p>

<h2>Lagos
<%= link_to '+', new_lago_path , class:"btn btn-primary"%>
</h2>

<table class="table">
  <thead>
    <tr>
      <th>Código</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lagos.each do |lago| %>
      <tr>
        <td><%= lago.id %></td>
        <td><%= link_to 'Edit', edit_lago_path(lago) , class:"btn btn-info"%></td>
        <td><%= link_to 'Destroy', lago, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>