<%= form_for(ponte) do |f| %>
  <% if ponte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ponte.errors.count, "error") %> prohibited this ponte from being saved:</h2>

      <ul>
      <% ponte.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :descricao , class:"control-label"%>
    <%= f.text_field :descricao , class:"form-control caixa"%>
  </div>

  <div class="form-group hidden">
    <%= f.label :geom , class:"control-label"%>
    <%= f.text_field :geom , class:"form-control caixa"%>
  </div>

  <div class="actions">
    <%= f.submit 'Salvar', class:"btn btn-primary" %>
  </div>
<% end %>
<br/>
